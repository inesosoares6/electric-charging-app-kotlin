<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/activity_charging_mode"
        android:id="@+id/chargingModePage"/>

    <include layout="@layout/charging"
        android:id="@+id/chargingPage"
        android:visibility="gone"/>

    <include layout="@layout/confirm_cancel"
        android:id="@+id/confirmCancelPage"
        android:visibility="gone"/>

    <include layout="@layout/finished"
        android:id="@+id/finishedPage"
        android:visibility="gone"/>

</androidx.constraintlayout.widget.ConstraintLayout>